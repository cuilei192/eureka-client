server:
  port: 8763

spring:
  application:
    name: eureka-client
  cloud:
    config:
      uri: http://config-server:8888/
      profile: dev
      label: master
eureka:
  instance:
    hostname: eureka-client
 